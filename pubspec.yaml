name: flutter_super_hero
description: Flutter is a Super Hero.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  database:
    path: database
  shared:
    path: shared
  domain:
    path: domain
  network:
    path: network
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
